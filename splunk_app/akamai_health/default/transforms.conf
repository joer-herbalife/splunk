# CSV lookup: ISO 3166-1 alpha-2 country code -> country name used by the
# geo_countries choropleth (featureIdField="country_name"). Replaces the ~170-line
# inline case() that previously lived in the map panel.
[country_iso_to_name]
filename = country_iso_to_name.csv
case_sensitive_match = 1
